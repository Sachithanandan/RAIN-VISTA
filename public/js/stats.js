// const folderPath = '../IR1/'; // This path should be relative to the server root
// const imageContainer = document.getElementById('imageContainer');

// // Fetch the list of images from the server
// fetch('/images')
//     .then(response => response.json())
//     .then(images => {
//         images.forEach(imageName => {
//             const imgElement = document.createElement('img');
//             imgElement.src = folderPath + imageName;
//             imgElement.alt = imageName;
//             imgElement.style.width = '200px'; // Set your desired width
//             imgElement.style.margin = '10px'; // Add some spacing between images

//             imageContainer.appendChild(imgElement);
//         });
//     })
//     .catch(error => console.error('Error fetching images:', error));
